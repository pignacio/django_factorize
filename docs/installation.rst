============
Installation
============

At the command line::

    $ easy_install django_factorize

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django_factorize
    $ pip install django_factorize
